# Build the admin shell layout

**Status:** todo

## What to do

Create the admin shell (layout with a sidebar + top bar) as Atomic Design components built on the design tokens, following the `vue-atomic-design` and `frontend-components` skills. Keep it mobile-first (~360px up) with touch targets ≥44px and no hardcoded values. This is the frame the login, products, and dashboards will render inside.

## Done criteria

The layout renders on mobile (~360px) and desktop with no horizontal overflow.

## Original line

> Criar o shell do admin (layout com barra lateral + topo) como componentes Atomic Design sobre os tokens. ✔ Layout renderiza em mobile (~360px) e desktop sem overflow horizontal.
