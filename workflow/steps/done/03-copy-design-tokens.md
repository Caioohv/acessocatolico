# Copiar tokens do design system

**Status:** done

## What to do

Copiar `design-system/tokens/*.css` e `design-system/styles.css` para dentro do portal, em `portal/app/assets/css/`, preservando a estrutura de pastas. Os arquivos de tokens devem ficar em `app/assets/css/tokens/`. Não alterar o conteúdo dos tokens nesta etapa, apenas trazê-los para o projeto.

## Original line

> - [ ] Copiar `design-system/tokens/*.css` e `design-system/styles.css` para `portal/app/assets/css/`. ✔ arquivos presentes em `app/assets/css/tokens/`.

## Summary

Copiados os 5 tokens (`colors`, `effects`, `fonts`, `spacing`, `typography`) de `design-system/tokens/` para `portal/app/assets/css/tokens/` e `design-system/styles.css` para `portal/app/assets/css/styles.css`, sem alterar conteúdo. Os `@import url('tokens/...')` de `styles.css` continuam válidos com a estrutura preservada.
