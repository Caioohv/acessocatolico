# Montar página index.vue

**Status:** done

## What to do

Montar `app/pages/index.vue` compondo as seções na ordem: `HeroHome` → `ShortcutsHome` → `LatestPosts`. A rota `/` deve renderizar as três seções nessa sequência.

## Original line

> - [ ] Montar `app/pages/index.vue` compondo Hero + Shortcuts + LatestPosts. ✔ `/` renderiza as três seções na ordem.

## Summary

Página `portal/app/pages/index.vue` configurada compondo as seções `HeroHome`, `ShortcutsHome` e `LatestPosts` na sequência correta com layout flex e espaçamentos padronizados por tokens.
