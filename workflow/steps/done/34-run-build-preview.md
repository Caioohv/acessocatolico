# Rodar build e preview

**Status:** done

## What to do

Rodar `npm run build` e depois `npm run preview`. O build deve concluir sem erros e o preview deve servir Home e Blog sem erros no console do navegador. Corrigir o que impedir o build/preview de passar.

## Original line

> - [ ] Rodar `npm run build` e `npm run preview`. ✔ build conclui e o preview serve Home + Blog sem erro no console.

## Summary

Executado `npm run build` com sucesso gerando os artefatos de servidor Nitro (.output) sem erros. Verificada a inicialização do preview de produção atendendo Home, Blog e detalhe de posts.
