{
  "name": "@acesso/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Fonte única do banco (schema, migrations e PrismaClient) compartilhada por portal/ e admin/.",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy"
  },
  "dependencies": {
    "@prisma/client": "7.10.0"
  },
  "devDependencies": {
    "prisma": "7.10.0"
  }
}
